const fs=require('fs');let s=fs.readFileSync('pages/retraissance/assets/site.js','utf8');s=s.replace(/\/\*[^]*?\*\//g,'').replace(/\/\/.*$/gm,'');const patterns=[/'(?:\\.|[^'\\])*'/g, /"(?:\\.|[^"\\])*"/g, /(?:\\.|[^\\])*/g];patterns.forEach(p=>{s=s.replace(p,'');});let depth=0;let line=1;let col=1;for(let i=0;i<s.length;i++){const ch=s[i];if(ch==='\n'){line++;col=1;continue;}if(ch==='{')depth++;else if(ch==='}')depth--;if(depth<0){console.log('depth<0 at',line,col);break;}col++;}
